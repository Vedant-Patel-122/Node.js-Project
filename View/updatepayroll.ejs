<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <title>Kiran Auto Parts And Services</title>
    <link rel="icon" type="image/x-icon" href="/logo7_12_92622.ico">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <style>
        body {  
            font-family: Arial, sans-serif;
            background-color: #f4f4f4;
            margin: 0;
            padding: 20px;
        }

        h3 {
            text-align: center;
            color: #34495e;
        }

        .form-container {
            background-color: #fff;
            padding: 20px;
            margin: 0 auto;
            max-width: 500px;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }

        .form-container h2 {
            text-align: center;
            color: #2980b9;
            margin-bottom: 20px;
        }

        label {
            display: block;
            margin-bottom: 5px;
            font-size: 14px;
        }

        input,
        select {
            width: 100%;
            padding: 10px;
            margin-bottom: 15px;
            font-size: 14px;
            box-sizing: border-box;
        }

        input[readonly] {
            background-color: #ecf0f1;
        }

        button {
            padding: 10px 20px;
            background-color: #27ae60;
            color: white;
            border: none;
            cursor: pointer;
            border-radius: 5px;
            font-size: 16px;
            display: block;
            margin: 0 auto;
        }

        @media (max-width: 600px) {
            .form-container {
                padding: 15px;
            }

            input,
            select {
                font-size: 15px;
                padding: 8px;
            }

            button {
                font-size: 15px;
                padding: 8px 16px;
            }
        }
    </style>
</head>

<body>

    <h3>Employee Attendance System</h3>

    <div class="form-container">
        <h2>Update Employee Payroll Data</h2>

        <form action="/updatepayroll" method="post">

            <label for="empid">Employee Id</label>
            <input type="number" id="empid" name="empid" value="<%= empid %>" readonly>

            <label for="workingday">Working Day</label>
            <input type="text" id="workingday" name="workingday" value="<%= workingday %>" readonly>

            <label for="status">Attendance Status</label>
            <select id="status" name="status">
                <option value="Present" <%= status === "Present" ? "selected" : "" %>>Present</option>
                <option value="Absent" <%= status === "Absent" ? "selected" : "" %>>Absent</option>
                <option value="Half Day" <%= status === "Half Day" ? "selected" : "" %>>Half Day</option>
                <option value="Holiday" <%= status === "Holiday" ? "selected" : "" %>>Holiday</option>
            </select>

            <label for="uppad">Uppad(Rs.)</label>
            <input type="number" id="uppad" name="uppad" step="0.01" value="<%= uppad %>">

            <label for="salaryperday">Salary Per Day(Rs.)</label>
            <input type="number" id="salaryperday" name="salaryperday" step="0.01" value="<%= salaryperday %>">

            <button type="submit">Update Payroll</button>
        </form>
    </div>

</body>

</html>
